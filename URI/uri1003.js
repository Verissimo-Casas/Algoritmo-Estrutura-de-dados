const PromptSync = require("prompt-sync")();

//Tipos de Dados

let Value1 = parseInt(PromptSync());
let Value2 = parseInt(PromptSync());

//Processamento

let SOMA = Value1 + V;

//Saida de Dados

console.log(`SOMA = ${SOMA}`);